sizeof(arr)	统计数组所占空间长度（如1个int为4）
sizeof(arr[1])	数组元素的内存

const int *p =&a;
常量指针
特点：指针的指向可以修改，但是指针指向的值不可以修改
（如：*p=20错误；*p=&a正确）

int *const p
指针常量
特点：指针的指向不可以修改，但是指针的值可以修改
区别：看const后面什么

<string.h>的头文件为<cstring>

EOF的用法
while(!(cin>>a>>b>>c>>d).eof())

cout<<R"()";括号里可以直接输出任何语句，而不用考虑\t\n(直接输出）
